# Install a CUDA-matched PyTorch build first in the target training environment.
# Example (CUDA 12.1):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Then install this file:
#   pip install -r requirements.txt

accelerate>=0.34.0
datasets>=2.19.0
huggingface-hub>=0.24.0
lm-eval>=0.4.9.2
llamafactory>=0.9.0
matplotlib>=3.8.0
numpy>=1.26.0
peft>=0.12.0
safetensors>=0.4.3
sentencepiece>=0.2.0
transformers>=4.44.0
trl>=0.10.1
